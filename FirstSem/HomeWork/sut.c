#include<stdio.h>
#include<math.h>
int main(){
    int u, t, a;
    float s,s1,s2;
scanf("%d, %d, %d, &f ", &u, &a, &t, &s);

s1= (u*t);
s2= 0.5 * a * pow(t,2);
s = s1 + s2 ;
printf("%f", s);

return 0;


}
#include<stdio.h>
int main(){
    int x;
    int y;
    int z;
    float w;
    float t;
    scanf ("%d, %d, %d %f %f", &x, &y, &z, &w, &t);
    printf("%%d, %d, %d %f %f", x,y, z, w, t);
    
    return 0;
    
    }
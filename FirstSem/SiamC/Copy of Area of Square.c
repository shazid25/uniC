#include<stdio.h>
int main ()
{
    int side,area;
    printf("The Value of one side is ");
    scanf("%d", &side);

    area = side*side;

    printf("\nThe area of Square is %d\n", area);
    return 0;
}
